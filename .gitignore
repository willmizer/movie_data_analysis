# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
venv/
.venv/
env/

# Environment variables
.env

# Jupyter
.ipynb_checkpoints/

# Embedding cache generated by tuning_weights.py (large, regenerated on demand)
modeling/embeddings.npz

# Scraper output files (regenerated by tmdb_scrape.py)
scraping/tmdb_movie_data_full.csv
scraping/tmdb_fetch_errors_full.csv

# Intermediate pipeline data (regenerated by pipeline scripts)
modeling/final_cleaned_tmdb.csv

# OS
.DS_Store
Thumbs.db

# Note: model artifacts (models/*.joblib, models/*.npz, models/*.pkl.gz)
# and scraping/combined_imdb_movies.csv are intentionally tracked —
# the Streamlit app loads them directly without re-running the pipeline.
